version: 2
updates:
  - package-ecosystem: "terraform"
    directories:
      - "/*"
    schedule:
      interval: "weekly"
    groups:
      terraform-modules:
        group-by: "dependency-name"
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
